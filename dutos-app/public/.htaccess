<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /DutOS/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /DutOS/index.html [L]
</IfModule>
